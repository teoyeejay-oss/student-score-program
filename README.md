# Student Score System

A Python OOP project demonstrating property decorators and exception handling.

## Features
- Set and validate student score with `@property` and `@setter`
- Auto-calculate grade based on score
- Raise and catch `ValueError` for invalid scores

## Concepts Used
- `@property` and `@setter`
- Exception Handling (`raise`, `try/except`)
